# BLDC Motor Configuration
# Standard settings for typical coil winding BLDC motor
# These parameters extend the [bldc_motor] section defined in the board config

[bldc_motor]
max_rpm: 4948.0    # Maximum motor RPM (spindle max / gear ratio)
min_rpm: 10.0
min_pwm_duty: 0.05

