# 5V Angle Sensor Configuration
# 12-bit ADC with 5V reference
# These parameters extend the [angle_sensor] section defined in the board config

[angle_sensor]
max_angle: 360.0
angle_auto_calibrate: True
saturation_threshold: 1.0  # Allow full range - no saturation warnings during calibration

